import tsconfig from "./tsconfig.json";
import eslintConfig from "./.eslintrc.json";
import prettierConfig from "./.prettierrc.json";

export { tsconfig, eslintConfig, prettierConfig };

const hello = (): number => {
  return 2;
};
